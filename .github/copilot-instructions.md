* не использовать namespace
* если ты заметил изменения в коде принимай их за основу, а не возвращайся к старой версии
* всегда проверяй файлы на ошибки компиляции
* никогда не добавлять null проверок для компонентов где их не было.